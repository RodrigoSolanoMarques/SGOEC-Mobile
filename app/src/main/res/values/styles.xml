<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorPrimary</item>
        <item name="android:windowBackground">@color/white</item>

        <item name="android:textColorPrimary">@color/black_21</item>
        <item name="android:textColorSecondary">@color/gray_75</item>
        <item name="android:divider">@color/gray_d9</item>
        <item name="android:icon">@color/white</item>

        <item name="android:listViewStyle">@style/ListViewStyle</item>


    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="FullscreenTheme" parent="AppTheme">
        <item name="android:actionBarStyle">@style/FullscreenActionBarStyle</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
        <item name="metaButtonBarStyle">?android:attr/buttonBarStyle</item>
        <item name="metaButtonBarButtonStyle">?android:attr/buttonBarButtonStyle</item>
    </style>

    <style name="FullscreenActionBarStyle" parent="Widget.AppCompat.ActionBar">
        <item name="android:background">@color/black_overlay</item>
    </style>

    <!-- ####################################################################################### -->

    <!-- Login -->
    <style name="LoginTheme" parent="AppTheme">
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColorPrimary">@color/colorAccent</item>
        <item name="android:textColorSecondary">@color/lightPrimaryColor</item>
    </style>

    <!-- ####################################################################################### -->

    <style name="ListViewStyle" parent="@android:style/Widget.ListView">
        <item name="android:background">@color/white</item>
        <item name="android:divider">@color/gray_d9</item>
    </style>

    <!-- ####################################################################################### -->


    <style name="DatePickerStyle" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:background">@color/lightPrimaryColor</item>
        <item name="colorAccent">@color/colorPrimary</item>
    </style>

    <!-- Text Input Layout -->
    <style name="StyleTextInputLayoutTheme">
        <item name="colorControlNormal">@color/colorPrimary</item> <!-- Cor padrão da linha -->
        <item name="colorControlActivated">@color/colorPrimary
        </item> <!--Cor linha e texto quando recebe foco-->
    </style>

    <style name="StyleTextInputLayout">
        <item name="android:theme">@style/StyleTextInputLayoutTheme</item>
        <item name="android:textColorHint">@color/colorPrimary
        </item> <!-- Cor texto(hint) sem foco -->
    </style>

    <!-- ####################################################################################### -->

    <!-- Edit Input -->
    <style name="StyleEditText">
        <item name="android:maxLines">1</item>
        <item name="android:maxLength">100</item>
        <item name="android:singleLine">true</item>
        <item name="android:textColor">@color/gray_75</item>
        <item name="android:inputType">textCapWords</item>
    </style>

    <!-- ####################################################################################### -->

    <style name="StyleEditTextDate">
        <item name="android:maxLines">1</item>
        <item name="android:focusable">false</item>
        <item name="android:singleLine">true</item>
        <item name="android:textColor">@color/gray_75</item>
        <item name="android:inputType">date</item>
    </style>

    <!-- ####################################################################################### -->

    <!-- Check Box -->
    <style name="StyleCheckBoxTheme">
        <item name="colorControlNormal">@color/lightPrimaryColor</item>
        <item name="colorControlActivated">@color/colorPrimary</item>
    </style>

    <style name="StyleCheckBox">
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:theme">@style/StyleCheckBoxTheme</item>
    </style>

</resources>
